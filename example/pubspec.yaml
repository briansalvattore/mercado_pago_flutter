name: mercado_pago_example
description: Demo from MercadoPago helper with FLutter.
version: 0.0.1

environment:
  sdk: ">=2.0.0-dev.68.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2
  mercado_pago:
    path : ../

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true